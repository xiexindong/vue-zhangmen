const getters = {
   classType: state => state.dictionary.classType
  // device: state => state.app.device,
  // roles: state => state.permission.roles,
  // tableHeight: state => state.settings.tableHeight,
}
export default getters
